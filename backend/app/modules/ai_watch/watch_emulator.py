"""
Эмулятор умных часов: вместо реального устройства просто генерируем
правдоподобные случайные данные. Дергается вручную (кнопка на фронте)
или можно повесить на APScheduler/cron, чтобы данные приходили "сами".

TODO(module-6): по желанию — фоновая задача, которая раз в N минут пишет
новую точку данных для активных пользователей.
"""
import random


def generate_data_point() -> dict:
    return {
        "steps": random.randint(0, 500),
        "heart_rate": random.randint(60, 150),
    }
